{
  "name": "affiliate-engine-v1",
  "version": "3.3.0",
  "private": true,
  "description": "NIKKSORA growth engine using Cloudflare Workers and D1.",
  "scripts": {
    "dev": "node scripts/inject-whatsapp-bot.mjs && node scripts/polish-site.mjs && node scripts/ux-fixes.mjs && wrangler dev",
    "build": "node scripts/inject-whatsapp-bot.mjs && node scripts/polish-site.mjs && node scripts/ux-fixes.mjs && tsc --noEmit",
    "deploy": "node scripts/inject-whatsapp-bot.mjs && node scripts/polish-site.mjs && node scripts/ux-fixes.mjs && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.44.0"
  }
}
